import mongoose, { Document, Schema } from 'mongoose';export interface ISession extends Document {    sessionId: string;    sessionName?: string;    userId: string;    ready: boolean;    connected: boolean;    qr?: string;    lastActivity: Date;    createdAt: Date;    updatedAt: Date;}const sessionSchema = new Schema<ISession>({    sessionId: {        type: String,        required: true,        unique: true,        index: true    },    sessionName: {        type: String,        required: false    },    userId: {        type: String,        required: true,        index: true    },    ready: {        type: Boolean,        default: false    },    connected: {        type: Boolean,        default: false    },    qr: {        type: String,        required: false    },    lastActivity: {        type: Date,        default: Date.now    }}, {    timestamps: true});export const Session = mongoose.model<ISession>('Session', sessionSchema);